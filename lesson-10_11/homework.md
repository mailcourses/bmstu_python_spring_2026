# Домашнее задание #10 (Pandas, Numpy)

### 1. Анализ датасета Titanic c Pandas
Во-первых, нужно установить `seaborn` и загрузить датасет `titanic`.

```python
df = seaborn.load_dataset("titanic")
```

Далее нужно выполнить подготовку данных:
- подсчитать число пропущенных значений по каждой колонке;
- заменить пропущенные значения `Age` на среднее значение возрастов;
- удалить все строки, где `Cabin` пропущен;
- заменить пропуски `Embarked` на модальное значение.

И несколько заданий:
1. Сколько пассажиров выжило?
2. Сколько женщин и мужчин было на борту?
3. Какова средняя стоимость билета по классам?
4. Какова медиана возраста по классам?
5. Какова корреляция между возрастом и выживаемостью?
6. Какова корреляция между размером семьи и выживаемостью? (понадобится информация нескольких столбцов)


### 2. Анализ продаж с Numpy
Нужно подготовить данные о продажах (10 товаров за 12 месяцев):
- вычисления должны быть воспроизводимыми;
- заполнить двумерный массив продаж (товары, месяцы) случайными целыми числами от 0 до 1000;
- заполнить массивы названий товаров и месяцев;
- вывести исходный массив продаж.

И провести анализ полученных данных о продажах:
1. Суммарные продажи по каждому товару (название товара: суммарные продажи)
2. Суммарные продажи по каждому месяцу (месяц: суммарные продажи)
3. Средние продажи по месяцам (месяц: средние продажи)
4. Количество месяцев, когда продажи превышали 500 (товар: количество месяцев)
5. Колебания спроса по формуле (Максимальные продажи - Минимальные продажи) / Средние продажи (товар: колебания)
6. Товар с наибольшим колебанием спроса из п5 (товар: значение)


### 3. Решение должно быть выполнено в jupyter notebook или аналоге
